export class TweetUser {
    id: string;
    name: string;
    dp: string;

    constructor(id: string, name: string, dp: string) {
        this.id = id;
        this.name = name;
        this.dp = dp;
    }
}